#search
  h1 Techlist #<%= ENV.fetch('CITY') %>

  ul.filters
    each controller
      li
        label for=id
          input type="checkbox" name="kind[]" id=id value=id checked=isSelected
            name
