#search
  h1 Techlist #<%= ENV.fetch('CITY') %>

  ul.filters
    each controller
      li
        label for=id
          view Ember.Checkbox id=id value=id checked=isSelected
            name

  .add
    p Still not on that map ? Add your place now !
    a href="/places/new" class="button" Add a place
