<% content_for(:meta_robots, 'noindex') if params[:page] %>

<%= render 'search' %>

<section id="search-results" class="places-list">
  <%= render 'places_list', places: @places, paginate_params: @paginate_params, results_one: 'lieu', results_other: 'lieux', list_path: places_path %>
</section>
